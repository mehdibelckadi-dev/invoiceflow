[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install -g pnpm@9 && pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @lefse/api build"]

[start]
cmd = "node apps/api/dist/index.js"
